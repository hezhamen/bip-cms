services:
  postgres:
    image: postgres:16-alpine
    ports:
      # Host 5432 often taken by local Postgres — use 5433
      - '5433:5432'
    environment:
      POSTGRES_USER: bip
      POSTGRES_PASSWORD: bip
      POSTGRES_DB: bip_cms
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U bip -d bip_cms']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
